{
  "name": "personal-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf ./docs && next build && next export -o ./docs && touch docs/.nojekyll && gh-pages -a -b master -d . --git \"$(which git)\""
  },
  "dependencies": {
    "framer-motion": "^1.3.0",
    "next": "9.0.2",
    "react": "16.8.6",
    "react-dom": "16.8.6"
  },
  "devDependencies": {
    "@types/node": "^12.6.8",
    "@types/react": "^16.8.23",
    "gh-pages": "^2.1.0",
    "typescript": "^3.5.3"
  }
}
